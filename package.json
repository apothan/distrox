{
  "name": "distrox",
  "version": "0.1.0",
  "description": "Distributed network build on IPFS",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "github.com/matthiasbeyer",
  "license": "GPL-2.0",
  "devDependencies": {
    "electron": "^5.0.3"
  }
}
